# OS-specific overrides (loaded from main config when present)
# macOS: font and window decorations (modeled from kitty os-specific.conf)

# Font - Maple Mono (match kitty)
font-size = 20.5
font-family = "Maple Mono Normal NL"
# adjust-cell-width = 90%
# adjust-cell-height = 40%

# Alternative: SpaceMono Nerd Font (uncomment to use, comment out Maple Mono above)
# font-size = 21
# font-family = "SpaceMono Nerd Font"
# adjust-cell-width = 90%
# adjust-cell-height = 40%

# Window - titlebar only (kitty: hide_window_decorations titlebar-only)
# Ghostty: toggle_window_decorations or leave default; no exact titlebar-only option in ref
